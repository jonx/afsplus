--- afsplus_start.c
+++ afsplus_start.c
@@ -72,0 +73 @@
+DECLARESET(LIBS)
@@ -92,2 +93,3 @@
-    if (set_call_funcs(SETNAME(INIT), 1, 1)) {
-        ret = handler(SysBase);
+    if (set_open_libraries()) {
+        if (set_call_funcs(SETNAME(INIT), 1, 1))
+            ret = handler(SysBase);
@@ -95,0 +98 @@
+    set_close_libraries();
