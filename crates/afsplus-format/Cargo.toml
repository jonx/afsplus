[package]
name = "afsplus-format"
description = "AFS+ on-disk structure encoding/decoding. no_std + alloc, no dependencies."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]

[lints]
workspace = true
