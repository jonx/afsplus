# Open Questions

Decisions that are deliberately unresolved. No agent settles one of them
implicitly by picking the easiest implementation; each is closed by an ADR
that records the decision, after which its row becomes a link to that ADR.
The plan that produces the deciding evidence is [ROADMAP.md](../ROADMAP.md)
Stage B; the review rules are in [AGENTS.md](../AGENTS.md).

| # | Question | Options | What decides it |
|---|---|---|---|
| Q1 | [Closed by ADR-062](../adr/ADR-062-explicit-hybrid-data-updates.md): explicit per-file hybrid, full COW by default, private in-place opt-in, shared or uncertain ranges always COW | Persistent policy encoding/API and portable-C parity remain M14 implementation gates, not architecture choices. | [Q1 bake-off](data-policy-bakeoff.md): random 4 KiB, database hot set, append, reflink, crash and injected-error evidence. |
| Q2 | [Closed by ADR-063](../adr/ADR-063-intent-log-epoch1.md): checkpoint COW plus bounded group commit and an intent log | The mechanism is accepted; version 3 now covers existing-file write/truncate through the Rust VFS/adapters, while portable-C parity, real-device flushes and wire freeze remain M14 gates. | [fsync requalification](fsync-intent-log-baseline.md) and [existing-file log gate](../testing/intent-log-write-truncate-qualification.md): namespace and data workloads with crash/recovery matrices. |
| Q3 | [Closed by ADR-067](../adr/ADR-067-epoch1-allocation-state.md): reserved triple-slot bitmap/descriptor representation, fixed `3N` allocation-root pool and segmented reclaim queue | The architecture is accepted; exact offsets and the global wire epoch remain M14 freeze work. Runtime emergency headroom is policy, not wire encoding. | The [allocation-state qualification](../testing/allocation-qualification.md) passes low-space forward progress, free/available reporting, crash gates and 1 GiB amplification bounds ([ROADMAP B1](../ROADMAP.md#b1-allocation-state)). |
| Q4 | [Direction selected by ADR-069](../adr/ADR-069-consistent-snapshots-first.md): consistent filesystem snapshots first, persistent under [ADR-070](../adr/ADR-070-persistent-snapshot-priority.md); what retention, admission and reclamation policy makes that promise bounded? | Persistent snapshot registry and protected ownership; explicit snapshot handles and unavailable/admission errors; measured retention limits. Individual-file version retention follows the snapshot consumer. | [Snapshot prototype S1–S4](../proposals/persistent-snapshot-prototype.md): use the [accounting model](../testing/book-review-qualification.md#snapshot-accounting-model), then qualify lifetime metadata, traversal past protected entries and metadata amplification in the integrated core. Registry, retention limits, mutation isolation and admission remain explicit decisions. See the [work queue](audit-work-queue.md). |
| Q5 | Rich security semantics (blocker 5): how much cross-platform ACL evaluation does the format define? | A versioned security-descriptor container that preserves unknown metadata across simpler hosts ([ADR-031](../adr/ADR-031-portable-security-acls.md), phase 1); full canonical NFSv4/Windows-like evaluation semantics. | Real POSIX and Windows mapping adapters validating or revising the canonical model ([ROADMAP Stage D](../ROADMAP.md#stage-d-portability-and-host-tooling), [testing/security-model-conformance.md](../testing/security-model-conformance.md)). |
| Q6 | Tiny-file storage ([ADR-018](../adr/ADR-018-inline-data-optional.md) reopened): which mechanism serves source trees and package caches? | Inline data; packed small-file storage; ordinary extents with strong locality. | A measured comparison on Cargo/Git-style trees before epoch 1 ([docs/22](../docs/22-pfs3-and-pfs4-lessons.md)). |
| Q7 | [Architecture closed by ADR-063](../adr/ADR-063-intent-log-epoch1.md): checkpoint COW is the sole metadata engine, with group commit and intent log layered above it | The exact checkpoint and log wire encodings remain M14 freeze gates rather than competing transaction architectures. | Real-device flush qualification, VFS/C interoperability and the independent format review named by ADR-020/063/064. |
| Q8 | Which proposed developer primitives stabilise? | Promising and general: stable object IDs, fast full-volume enumeration, persistent change discovery, `CloneFile`/`CloneRange`, bulk metadata operations, explicit durability semantics, explain/health interfaces, access-intent hints. Experimental: `AtomicBatch()`, `SealContent()`, the content-inspection feed, security domains, an online reverse map, recursive directory statistics, a content fingerprint cache, `CloneTree`. | One real application consumer and measured value per primitive; a plausible API is not enough (the Windows TxF lesson). Catalog and change stream are the strongest bets because centralised enumeration and change journals have extensive precedent ([ROADMAP Stage E](../ROADMAP.md#stage-e-developer-contract-accelerators-and-optional-features)). |
| Q9 | Proposals awaiting team review | Tool contract D1–D7, storage media profiles M1–M5, checkpoint slot rings ([proposals/](../proposals/README.md)). | The team's review; on acceptance each lands as an ADR, design document or spec file. |
| Q10 | What is the epoch-1 rule for the checkpoint's currently reserved `flags` word? | Require zero and reject any nonzero value; expose a negotiated flag namespace; remove the field before freeze. Neither Rust nor portable C assigns semantics to it. | M14 independent format review: every retained reserved field must have an explicit reject, ignore or negotiation rule before the wire layout freezes. |
| Q11 | What is the first supported damaged-volume salvage and backup/restore contract? | Read-only extraction with explicit loss report; transactional repair of selected invariant classes; complete metadata-preserving restore. | Extend the [extraction corpus](../testing/extraction-qualification.md) and build an actual archive/restore consumer; specify recoverable damage classes, destination isolation, exact metadata preservation and refusal behavior. See [coverage map](../testing/book-review-qualification.md#normative-coverage-map). |
| Q12 | What storage-stack failure and cache contract must each writable host qualify? | Provider-specific flush guarantees with explicit limits; additional device capability negotiation where necessary. | Complete-write errors, delayed/failed completion, cache coherence, DMA shutdown and sleep/power interruption on the target. Host simulations remain separate from device evidence. |
